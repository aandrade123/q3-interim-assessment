function setup(){
    createCanvas(600,400);
}

function draw(){
    background(95 , 78, 190);
    //display ball
    fill(50, 250,50);
    ellipse(x, 100, 60,60);
    //make ball move
    x + x + speedx;
    //ball changes direction when it hits wall
    
}